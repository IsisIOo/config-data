server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/orders-db
    username: postgres
    password: isilinda
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka